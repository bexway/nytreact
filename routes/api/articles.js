const router = require("express").Router();
const booksController = require("../../controllers/articlesController");

// router.route("/api/saved")
//  .get()//route for retrieving all saved articles from db
//  .post()//route for for adding saved articles to db
//  .delete;//route for deleting saved articles from db

module.exports = router;
